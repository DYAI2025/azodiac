---
description: Comprehensive VPS health check — services, agents, memory, disk, zombies
allowed-tools: Bash, Read, AskUserQuestion
---

## Context

The DYAI VPS (srv1308064.hstgr.cloud) runs multiple agents (Perr00bot, Marvin, OpenClaw) and services. Memory usage frequently creeps above 90%, zombie processes accumulate, and services occasionally go down. This skill provides a full health overview.

SSH: `ssh -i ~/.ssh/id_ed25519 root@srv1308064.hstgr.cloud`

## Arguments

- `$ARGUMENTS` — Optional: "quick" for summary only, or specific check like "memory", "agents", "zombies"

## Your Task

Run a comprehensive health check on the VPS and report status clearly.

### Steps

1. **System Resources:**
   ```bash
   ssh -i ~/.ssh/id_ed25519 root@srv1308064.hstgr.cloud "free -h && echo '---' && df -h / && echo '---' && uptime"
   ```

2. **Running Agents:**
   ```bash
   ssh -i ~/.ssh/id_ed25519 root@srv1308064.hstgr.cloud "ps aux | grep -E '(perr00bot|marvin|openclaw|node|python)' | grep -v grep"
   ```

3. **Systemd Services:**
   ```bash
   ssh -i ~/.ssh/id_ed25519 root@srv1308064.hstgr.cloud "systemctl list-units --type=service --state=running | grep -E '(perr00|marvin|openclaw|gateway|kanban|cron)'"
   ```

4. **Zombie/Orphan Processes:**
   ```bash
   ssh -i ~/.ssh/id_ed25519 root@srv1308064.hstgr.cloud "ps aux | awk '\$8 ~ /Z/ {print}' && echo '---ORPHANS---' && ps aux --sort=-%mem | head -20"
   ```

5. **Memory Sync Cron:**
   ```bash
   ssh -i ~/.ssh/id_ed25519 root@srv1308064.hstgr.cloud "crontab -l 2>/dev/null | grep memory"
   ```

6. **Report** — Present a clear summary table:
   ```
   ┌────────────┬──────────┬─────────────────────┐
   │  Component │  Status  │  Details            │
   ├────────────┼──────────┼─────────────────────┤
   │  Memory    │  OK/WARN │  X/Y GB used (Z%)   │
   │  Disk      │  OK/WARN │  X/Y GB used (Z%)   │
   │  Perr00bot │  UP/DOWN │  PID XXXX           │
   │  Marvin    │  UP/DOWN │  PID XXXX           │
   │  OpenClaw  │  UP/DOWN │  PID XXXX           │
   │  Zombies   │  N found │  [details if any]   │
   └────────────┴──────────┴─────────────────────┘
   ```

### Guardrails

- Read-only checks only — never kill processes or restart services without asking
- If memory > 85%, flag it prominently and suggest `/claude-process-manager` for cleanup
- If an expected agent is down, ask user before restarting

---
*Generated by /reflect-skills from 5+ session patterns across 3 sessions*
